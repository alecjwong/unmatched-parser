JSON files are to be downloaded from the TTS JSON tab under export for fan decks on unmatched.cards

JSON files that have been resaved on TTS may not work properly with the program

Output file will be placed in the same folder as the input folder